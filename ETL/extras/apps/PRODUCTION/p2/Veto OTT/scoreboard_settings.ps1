# Change these values before starting the scoreboard when a different port is needed.
$ScoreboardHost = "0.0.0.0"
$ScoreboardPort = 8080
$AllowedRemoteAddresses = @(
    "127.0.0.1",
    "192.168.0.0/16"
)
